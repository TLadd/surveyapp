const MainSurvey = require('./components/main-survey');
const React = require('react');
const ReactDOM = require('react-dom');

ReactDOM.render(
  <MainSurvey/>,
  document.getElementById('survey-app')
);
